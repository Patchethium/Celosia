# English phonemizer example

This example demonstrates how to use the English phonemizer in Celosia.

```rust
cargo run -p demo-en
cargo run -p demo-en --release // for realistic performance
```

It will start a GUI application powered by `egui`.